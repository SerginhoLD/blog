<?php
use Slim\Views\PhpRenderer;
use Blog\Model\PostInterface;
use Blog\Markdown;

/**
 * @var PhpRenderer $this
 * @var PostInterface[] $posts
 * @var int $page
 * @var int $totalPages
 * @var Markdown\ParserInterface $markdown
 */

var_dump([
    'page' => $page,
    'totalPages' => $totalPages,
]);

foreach ($posts as $post)
{
    var_dump([
        'id' => $post->getId(),
        'slug' => $post->getSlug(),
        'name' => $post->getName(),
        'preview' => $markdown->parse($post->getPreview()),
        'tags' => $post->getTags(),
    ]);
}
