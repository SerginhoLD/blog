<?php
use Blog\Model\PostInterface;

/**
 * @var PostInterface $post
 */

var_dump([
    'id' => $post->getId(),
    'slug' => $post->getSlug(),
    'name' => $post->getName(),
    'text' => $post->getTextHtml(),
    'tags' => $post->getTags()->toArray(),
]);
